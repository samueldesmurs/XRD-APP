# XRD-APP
# Perovskite Peak Analyzer (Python)  A Python application to **identify** and **quantify diffraction peaks** in perovskite-related datasets (e.g., XRD / GIWAXS).   It provides an interactive workflow for **peak detection**, **peak fitting**, **phase labeling**, and **export of results** (CSV/Excel) for further analysis and reporting.
